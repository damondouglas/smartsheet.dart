import 'package:test/test.dart';

main() {
  group('users.dart', () {
    group('current', () {
      test('should acquire current loggedin user', () {
        expect(1, 1);
      });
    });
  });
}
